# Unity Project — Additional Interview Questions

Ask these after the common questions (Q1–Q3). One question at a time.
If the user answers "skip", "pass", "s", or "모름", mark the item as `[needs confirmation]` and move on.

```
Q4. Unity version and render pipeline? (URP / HDRP / Built-in)
Q5. Target platform? (PC, Mobile, WebGL, Console)
Q6. Scene structure? (single scene, multi-scene, additive loading)
Q7. Key manager scripts? (GameManager, UIManager, NetworkManager, etc.)
Q8. Assembly definitions (*.asmdef) layout?
Q9. Third-party packages or assets in use?
Q10. REST API server integration? If yes, base URL and auth method?
Q11. Build pipeline? (Unity Cloud Build, Jenkins, manual, etc.)
```
