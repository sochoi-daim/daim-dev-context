# Frontend Project — Additional Interview Questions

Ask these after the common questions (Q1–Q3). One question at a time.
If the user answers "skip", "pass", "s", or "모름", mark the item as `[needs confirmation]` and move on.

```
Q4. Which framework and version? (React / Next.js / Vue / other)
Q5. Routing approach? (React Router, Next.js App Router/Pages Router, Vue Router, etc.)
Q6. State management? (Redux, Zustand, Pinia, Context API, etc.)
Q7. Styling approach? (Tailwind, CSS Modules, styled-components, Sass, etc.)
Q8. API communication? (REST, GraphQL, tRPC) and HTTP client? (fetch, axios, etc.)
Q9. Authentication method? (JWT, session, OAuth, etc.)
Q10. Key environment variables? (.env.example or equivalent)
Q11. Build tool and deployment target? (Vite, Webpack, Vercel, Docker, etc.)
```
