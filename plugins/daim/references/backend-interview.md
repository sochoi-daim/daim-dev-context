# Backend Project — Additional Interview Questions

Ask these after the common questions (Q1–Q3). One question at a time.
If the user answers "skip", "pass", "s", or "모름", mark the item as `[needs confirmation]` and move on.

```
Q4. Which framework and version? (Express / NestJS / Fastify / other)
Q5. Language and runtime? (TypeScript / JavaScript, Node.js version)
Q6. Database? (PostgreSQL, MySQL, MongoDB, etc.) and ORM/ODM? (Prisma, TypeORM, Mongoose, etc.)
Q7. Authentication method? (JWT, session, OAuth, API key, etc.)
Q8. API style? (REST, GraphQL, gRPC)
Q9. Key middleware or interceptors?
Q10. Testing setup? (Jest, Vitest, Supertest, etc.)
Q11. Deployment target? (Docker, AWS, GCP, etc.) and CI/CD pipeline?
```
